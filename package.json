{
  "name": "launch-tracker",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "npx -y serve . -p 3000",
    "build": "mkdir -p dist && cp index.html dist/",
    "start": "npx -y serve dist -p 3000"
  }
}
